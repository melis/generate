import { useState } from "react";
import "./App.css";

function App() {
  const [x, setX] = useState({ name: "dsd", seats: [{ id: 1 }] });

  return <div className="App">фзз</div>;
}

export default App;
